import React from 'react';
import { View } from 'react-native';

// Displays waiting for players after creating new game, or player disconnected
const PlayerLoader = () => {
  return <View></View>;
};

export default PlayerLoader;
